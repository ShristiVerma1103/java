// program to sum to number from user
#include<stdio.h>
void main()
{
    // variable declaration
    int a, b, sum=0;       
    printf(" enter the two digit for sum");

    //input of two numbers from user
    scanf("%d %d", &a, &b);

    // sum operation
    sum = a+b;

}